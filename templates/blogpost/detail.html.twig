{% extends 'base.html.twig' %}

 {% block title %}Actualites{% endblock %}

{% block body %}
 
    <!-- ARTICLES -->
    <section class="pb-8 pt-md-11">
      <div class="container">
        <div class="row justify-content-center">
          <div class="col-12 col-md-10 col-lg-9 col-xl-8">

            <!-- Heading -->
            <h1 class="display-4 text-center">
              {{ blogpost.titre }}
            </h1>
            <div class="row align-items-center py-5 border-top border-bottom">
              <div class="col-auto">
              </div>
              <div class="col ms-5">
                <time class="fs-sm text-muted" datetime="2021-11-20">Publié le {{ blogpost.createdAt|date('d/m/Y') }}</time>
              </div>
            </div>
                
            </div>
          </div>
        </div>
      </section>

      <section class="pt-6 pt-md-8">
        <div class="container">
          <div class="row justify-content-center">
            <div class="col-12 col-md-10 col-lg-9 col-xl-8">
              <p class="mb-6">
                {{ blogpost.contenu }}
              </p>
            </div>
          </div>
        </div>
      </section>

      {% include "commentaire/form.html.twig" %}
      {% include "commentaire/commentaires.html.twig" %}
      
{% endblock %}
